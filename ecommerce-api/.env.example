# Database
DATABASE_URL=postgresql://admin:password123@localhost:5432/ecommerce

# JWT
# 🐛 INTENTIONAL ISSUE: This should NOT be in the repository
# These secrets should be generated per environment and never committed
JWT_SECRET=super-secret-key-12345
JWT_EXPIRATION=7d

# Server
PORT=3000
NODE_ENV=development

# API Keys (examples - replace with real keys)
STRIPE_API_KEY=sk_test_your_stripe_key_here
SENDGRID_API_KEY=SG.your_sendgrid_key_here

# SECURITY NOTE FOR WORKSHOP:
# In a real project, this file should be .env (not .env.example)
# and it should be in .gitignore!
# Never commit real secrets to version control.
